/// 機能フラグ（コンパイル時定数）
///
/// 役割:
/// - 実装は残したまま、まだ出さない画面・機能を一箇所で切り替える
/// - 値を変えるだけで露出を切り替えられるよう、画面側はこのクラスだけを見る
///
/// クリーンアーキテクチャにおける位置づけ:
/// - Shared 層の設定。どの層からも参照してよい（ビジネスロジックは持たない）
class FeatureFlags {
  FeatureFlags._();

  /// 通知タブ内の「お知らせ」ページを出すか（Issue #81）
  ///
  /// 右タブ（運営・ジムからのお知らせ）は仕様が固まっていないため当面は隠す。
  /// DB・API・画面の実装は残してあり、true にすれば「通知｜お知らせ」の 2 タブになる
  static const bool showAnnouncementsTab = false;
}
